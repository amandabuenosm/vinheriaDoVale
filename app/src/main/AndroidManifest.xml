<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.vinheria">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vinicola"
        tools:targetApi="31">

        <activity
            android:name=".LoginActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ManutencaoClientesActivity" />
        <activity android:name=".ManutencaoPedidosActivity" />
        <activity android:name=".ClientesCadastradosActivity" />
        <activity android:name=".MetasComissoesActivity" />
        <activity android:name=".PedidosCadastradosActivity" />
        <activity android:name=".DetalheClienteActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".CheckoutActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".OrderConfirmationActivity" />
        <activity android:name=".ProductDetailActivity" />
        <activity android:name=".NovoClienteActivity" />
        <activity android:name=".NovoPedidoActivity" />
        <activity android:name=".DispEstoqueActivity"/>
    </application>

</manifest>
